spring:
  application:
    name: web-app

---
spring:
  profiles: local
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

---
spring:
  profiles: dt
eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka/

---
spring:
  profiles: dp
eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka/

---
spring:
  profiles: production
eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka/