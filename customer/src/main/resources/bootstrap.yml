spring:
  application:
    name: customer

---
spring:
  profiles: local
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

---
spring:
  profiles: dev
eureka:
  client:
    service-url:
      defaultZone: http://172.16.99.128:8761/eureka/

---
spring:
  profiles: dt
eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka/

---
spring:
  profiles: dp
eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka/

---
spring:
  profiles: production
eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka/